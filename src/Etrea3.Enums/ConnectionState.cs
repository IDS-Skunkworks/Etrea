namespace Etrea3
{
    public enum ConnectionState
    {
        MainMenu,
        CreatingCharacter,
        GetUserName,
        GetPassword,
        Playing,
        InOLC,
        Disconnected
    }
}